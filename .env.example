PORT=5585
NODE_ENV=production
SESSION_SECRET=super-secret-key-replace-with-random-bytes
DATA_DIR=./data
UPLOADS_DIR=./uploads
REQUIRE_HTTPS=false

# Optional: Subpath deployment setting (e.g. BASE_PATH=/hausmeister if deployed under a subpath behind Nginx)
# BASE_PATH=/hausmeister
